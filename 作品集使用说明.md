# AI 商品图生成器｜作品集使用说明

这是一个可在本机运行的 AI 电商图片工作台。使用者可为同一商品建立多个独立生成任务，分别设置图片类型、输出比例、精确像素、提示词和图片数量，然后一次生成并管理结果。

## 如何启动

1. 安装 Node.js 20 或更高版本。
2. 解压交付包。
3. macOS 双击 `启动 AI 商品图生成器.command`。
4. 首次启动会自动安装依赖，随后打开 `http://127.0.0.1:4317/`。

如果 macOS 拦截启动脚本，在 Finder 中右键文件并选择“打开”。

## 两种演示方式

### 无 API 演示

在“模型设置”中选择“演示模式”。可完整展示素材库、多任务、独立尺寸、批量生成状态、生成记录、详情和单图下载。演示图会明确标记，不会冒充真实 AI 生成结果。

### 真实生图

二选一：

- 选择 API，填写自己的 Base URL、API Key 和图片模型 ID。官方 OpenAI 地址为 `https://api.openai.com/v1`；第三方中转站必须兼容 OpenAI 图片编辑接口。
- 选择 Codex 桌面 Agent，先测试本机安装、登录、网络和图片能力，通过后再生成。

交付包不包含任何 API Key、个人生成记录或本机账号信息。

## 作品集展示建议

建议按“问题—方案—流程—验证—迭代”讲解：

1. 问题：同一商品需要同时产出不同用途和尺寸的电商图，传统操作需要反复切换和输入。
2. 方案：把每种出图要求建模为独立任务，再统一提交和管理结果。
3. 流程：导入素材 → 建立任务 → 选择尺寸/数量 → 选择 AI 来源 → 生成 → 历史详情/下载。
4. 验证：下载图片的实际像素与任务选择一致，失败任务可单独重试。
5. 迭代：说明从单图原型迭代到多任务、多尺寸、可配置 API 和可追溯历史的过程。

只陈述自己真实参与的工作，不要将未参与的设计、开发或测试写成个人成果。
